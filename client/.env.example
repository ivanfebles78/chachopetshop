# En desarrollo se deja vacío: Vite hace proxy de /api al servidor (localhost:4000).
# En producción con frontend separado (Cloudflare Pages), apunta a la API de Railway:
# VITE_API_URL="https://tu-api.up.railway.app"
VITE_API_URL=""
